# 本版验证记录

参考运行日期：2026-09-20。Python 3.12.14，NumPy 2.3.5；精确配置与计算脚本校验值见results/reference/run_manifest.json。

- 模拟器18项单元测试全部通过，覆盖解析时延、缓存复用与淘汰、跨后端缓存隔离、切换阈值、候选数取整、连续批容量、容量约束、守恒和确定性。
- 请求路由：10个场景 × 3个种子 × 4种策略，共120次运行、216000条请求记录。
- EPLB：7个场景 × 3个种子 × 3种策略，共63次运行、22680步记录。
- audit_results.py从原始CSV重新核算时序、TTFT分解、均值、P95、APC比例、迁移耗时与守恒，并检查30组公共请求轨迹和计算源码哈希，全部通过。见results/reference/audit.json。
- 四个数学核验脚本通过：5796个副本子问题、19999个分流网格点、1000个一阶条件检查、各50000组快照及改派误差检查、缓存服务矩检查、5000组切换阈值检查。输出位于results/verification。
- 将代码复制到另一目录，在独立工作目录运行steady_16场景、种子31、算法二：轨迹哈希与参考数据一致，结果指标在1e-12容差内相同。

复现结果为合成输入下的模型结果。生产中算法一TTFT降低10%的结果与这些模拟分别记录。图表和主要结论见results/reference/RESULTS.md；模型、参数和统计口径见docs/MODEL.md。

MANIFEST.sha256列出本次归档的全部文件校验值（不包含清单自身）。目录可整体移动，按README命令重新运行。
